# Mingyue Xu - Resume

Compile the resume from this directory with:

```sh
latexmk -pdf Mingyue_Xu_Resume.tex
```

The main document is `Mingyue_Xu_Resume.tex`; reusable styling and layout commands are in `resume.cls`; section content is kept in `sections/`.

